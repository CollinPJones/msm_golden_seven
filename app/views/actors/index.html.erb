<nav class="navbar navbar navbar-default navbar-fixed-top">

  <div class="navbar-header">
    <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
      <span class="sr-only">Toggle navigation</span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>

    <a href="/" class="navbar-brand">MovieDB</a>
  </div>

  <div class="navbar-collapse collapse" id="bs-example-navbar-collapse-1" aria-expanded="false" style="height: 0.8px;">
    <ul class="nav navbar-nav">
      <li>
        <a href="/directors">Directors</a>
      </li>
      <li>
        <a href="/movies">Movies</a>
      </li>
      <li>
        <a href="/actors">Actors</a>
      </li>
    </ul>
  </div>
</nav>

<div class="container">
  <div class="page-header">
    <h1>Actors</h1>
  </div>
  <p>
    <a href="/actors/new_form" class="btn btn-primary">Add a New Actor</a>
  </p>
  <table class="table table-hover">
    <thead>
      <tr>
        <th>Image</th>
        <th>Dob</th>
        <th>Name</th>
        <th>Bio</th>
        <th></th>
        <th></th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <% @actor.each do |actor| %>
      <tr>
        <td><img src="<%= actor.image_url %>" width="80"/></td>
        <td><%= actor.dob %></td>
        <td><%= actor.name %></td>
        <td><%= actor.bio %></td>
        <td><a href="/actors/<%= actor.id %>" class="btn btn-primary">S</a></td>
        <td><a href="/actors/<%= actor.id %>/edit" class="btn btn-warning">E</a></td>
        <td><a href="/delete_actor/<%= actor.id %>" class="btn btn-danger">D</a></td>
      </tr>
      <% end %>
    </tbody>
  </table>
</div>
